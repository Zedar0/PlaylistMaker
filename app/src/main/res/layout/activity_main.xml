<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingTop="@dimen/dimention_16dp"
    android:paddingHorizontal="@dimen/dimention_16dp"
    android:paddingBottom="@dimen/dimention_28dp"
    android:background="?attr/colorPrimary">

    <TextView
        style="@style/MainTextViewStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:text="@string/app_name"
        android:textSize="@dimen/text_size_22"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:showDividers="middle"
        android:divider="@drawable/empty_tall_divider"
        android:orientation="vertical"
        android:layout_marginTop="@dimen/dimention_40dp"
        >

        <Button
            android:id="@+id/button_search"
            style="@style/ButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="@string/search_text"
            android:textSize="@dimen/text_size_22"
            app:icon="@drawable/search_icon"
            android:foreground="?attr/selectableItemBackground"/>

        <Button
            android:id="@+id/button_library"
            style="@style/ButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="@string/medialibrary_text"
            android:textSize="@dimen/text_size_22"
            app:icon="@drawable/medialibrary_icon"
            android:foreground="?attr/selectableItemBackground"/>
        <Button
            android:id="@+id/button_setting"
            style="@style/ButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="@string/settings_text"
            android:textSize="@dimen/text_size_22"
            app:icon="@drawable/settings_icon"
            android:foreground="?attr/selectableItemBackground"/>
    </LinearLayout>
</LinearLayout>